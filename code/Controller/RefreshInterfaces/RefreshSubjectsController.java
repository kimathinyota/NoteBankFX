package Code.Controller.RefreshInterfaces;

/**
 * Controllers that need to react to a change in subjects (addition, deletion, modification)
 */
public interface RefreshSubjectsController {
    void refreshSubjects();
}
